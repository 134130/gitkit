# gitkit

Shared Go packages for running `git` and `gh` commands and building small GitHub CLI tools.

## Packages

- `gitcmd`: process execution boundary for `git` and `gh`
- `gitrepo`: local Git repository operations built on `gitcmd`
- `ghcli`: low-level `gh` command helpers
- `ghpr`: pull request semantics built on `ghcli`
